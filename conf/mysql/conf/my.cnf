!includedir /etc/mysql/conf.d/
!includedir /etc/mysql/mysql.conf.d/
[mysqld]
max_connections=1000
# Disable TLS by clearing the supported protocol list instead of using the
# deprecated --ssl flag. This keeps the previous behaviour of exposing MySQL
# without TLS while satisfying the newer server recommendation.
tls_version=""
